// https://app.codesignal.com/arcade/intro/level-2/xskq4ZxLyqQMCLshr

function matrixElementsSum(matrix) {
  let sum = 0
  for(let i=0;i<matrix[0].length;i++) {
      for(let j=0;j<matrix.lengh;j++) {
          if (matrix[i][j])
      }   
  }
}

// for the first element of the first Array
// -if it's not zero, add it to sum, then check first element of second Array. Repeat through the final array
// -if it's zero skip all first elements and go to second element of first Array
// -if it's not zero, add it to sum, then check second element of second Array. Repeat through the last array